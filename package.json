{
  "name": "nesdz",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test && python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
